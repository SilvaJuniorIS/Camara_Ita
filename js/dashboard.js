(function(){
  function metrics(){const p=AppStorage.load('progress'),a=Object.values(AppStorage.load('answers')),s=AppStorage.load('study_sessions'),r=AppStorage.load('reviews'),correct=a.filter(x=>x.correct).length,hours=s.reduce((n,x)=>n+x.duration,0)/60;return{progress:Math.round(p.completedChapters.length/10*100),answered:a.length,accuracy:a.length?Math.round(correct/a.length*100):0,hours:hours.toFixed(1),reviews:r.filter(x=>x.status==='pendente').length,streak:streak(s)}}
  function streak(s){const days=new Set(s.map(x=>x.date.slice(0,10)));let n=0,d=new Date();while(days.has(d.toISOString().slice(0,10))){n++;d.setDate(d.getDate()-1)}return n}
  function page(){const m=metrics(),user=AppStorage.load('user'),p=AppStorage.load('progress'),next=Math.min(10,(p.completedChapters.at(-1)||0)+1),schedule=AppStorage.load('schedule').slice(0,4);return `<section class="welcome-banner"><div><span class="eyebrow light">${new Date().toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}).toUpperCase()}</span><h1>Olá, ${Navigation.escapeText(user.name)}.</h1><p>Você está construindo sua aprovação, uma sessão por vez.</p></div><p class="quote" id="motivation">“A constância transforma intenção em resultado.”</p></section><div class="grid grid-4">${stat(`${m.progress}%`,'Progresso do curso','↗'),stat(`${m.hours}h`,'Horas estudadas','◷'),stat(m.answered,'Questões respondidas','✓'),stat(`${m.accuracy}%`,'Média de acertos','◎'),stat(`${m.streak} dias`,'Sequência de estudos','◆'),stat(m.reviews,'Revisões pendentes','↻'),stat('18h30','Meta semanal','□'),stat(`${p.completedChapters.length}/10`,'Capítulos concluídos','▤')}</div><div class="dashboard-layout" style="margin-top:18px"><div class="grid"><article class="card next-card"><div class="card-header"><div><span class="eyebrow">Próximo conteúdo</span><h2 style="font-size:19px;margin-top:6px">Capítulo ${next}: ${CourseData.modules[0].chapters[next-1]||'Revisão final'}</h2><span class="card-subtitle">Módulo 0 — Método e planejamento</span></div><span class="badge success">Recomendado</span></div><div class="progress"><i style="width:${m.progress}%"></i></div><a class="button button-primary" style="margin-top:16px" href="capitulo.html?id=${next}">Continuar estudando →</a></article><div class="grid grid-2"><article class="card"><div class="card-header"><div><h2>Evolução semanal</h2><span class="card-subtitle">Horas registradas</span></div></div>${bars([0,0,0,0,0,0,Number(m.hours)],['S','T','Q','Q','S','S','D'])}</article><article class="card"><div class="card-header"><div><h2>Desempenho por disciplina</h2><span class="card-subtitle">Percentual de acertos</span></div><a href="desempenho.html" class="card-subtitle">Ver análise</a></div>${disciplineRows()}</article></div><article class="card"><div class="card-header"><h2>Últimas atividades</h2></div><div class="activity-list">${activityList()}</div></article></div><aside class="grid"><article class="card"><div class="card-header"><div><h2>Agenda do dia</h2><span class="card-subtitle">Seu plano de ação</span></div><a href="planner.html" class="card-subtitle">Editar</a></div>${schedule.length?schedule.map(t=>`<div class="agenda-item"><time>${t.duration} min</time><div><b>${Navigation.escapeText(t.title)}</b><span>${Navigation.escapeText(t.discipline)}</span></div></div>`).join(''):'<div class="empty-state"><b>Agenda livre.</b><a href="planner.html">Planejar agora</a></div>'}</article><article class="card pomodoro-mini"><div class="card-header"><h2>Foco Pomodoro</h2><span class="badge">25 min</span></div><div class="timer-display">25:00</div><p class="card-subtitle">Uma tarefa. Sem interrupções.</p><button class="button button-primary" data-action="open-pomodoro" style="margin-top:12px">Iniciar sessão</button></article><article class="card"><div class="card-header"><h2>Revisões</h2><span class="badge ${m.reviews?'warning':'success'}">${m.reviews} pendentes</span></div><p class="card-subtitle">O sistema agenda revisões 24h, 7d e 30d após cada capítulo.</p><a class="button button-secondary button-sm" href="revisoes.html" style="margin-top:12px">Ver revisões</a></article></aside></div>`}
  function stat(v,l,i){return `<article class="card stat-card"><span class="stat-icon">${i}</span><div><b>${v}</b><span>${l}</span></div></article>`}
  function bars(values,labels){const max=Math.max(1,...values);return `<div class="chart-bars">${values.map((v,i)=>`<div class="chart-bar"><i style="height:${Math.max(3,v/max*100)}%"></i><span>${labels[i]}</span></div>`).join('')}</div>`}
  function disciplineRows(){const answers=Object.values(AppStorage.load('answers'));const disciplines=['Estratégias de prova','Língua Portuguesa','Administração Pública','Informática','Matemática'];return disciplines.map(d=>{const x=answers.filter(a=>a.discipline===d),v=x.length?Math.round(x.filter(a=>a.correct).length/x.length*100):0;return `<div style="margin-bottom:12px"><div style="display:flex;justify-content:space-between;font-size:10px"><span>${d}</span><b>${v}%</b></div><div class="progress"><i style="width:${v}%"></i></div></div>`}).join('')}
  function activityList(){const s=AppStorage.load('study_sessions').slice(-3).reverse();if(!s.length)return '<div class="empty-state"><b>A jornada começa aqui.</b><p>Inicie uma sessão de foco para registrar sua primeira atividade.</p></div>';return s.map(x=>`<div class="activity"><i></i><div><b>${x.duration} minutos de ${x.type}</b><span>${x.discipline} • ${new Date(x.date).toLocaleDateString('pt-BR')}</span></div></div>`).join('')}
  window.Dashboard={page,metrics,bars};
})();
