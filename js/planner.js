(function(){
  function seed(){if(AppStorage.load('schedule').length)return;const types=['teoria','questões','revisão','flashcards'];const disciplines=['Método e planejamento','Língua Portuguesa','Matemática','Informática','Administração Pública'];const start=new Date();const tasks=[];for(let i=0;i<90;i++){const date=new Date(start);date.setDate(start.getDate()+i);const day=date.getDay();const duration=day===6?240:day===0?120:150;tasks.push({id:Date.now()+i,date:date.toISOString().slice(0,10),title:`${types[i%types.length]} — ${disciplines[i%disciplines.length]}`,discipline:disciplines[i%disciplines.length],duration,type:types[i%types.length],priority:i%5===0?'alta':'normal',status:'pendente',notes:''})}AppStorage.save('schedule',tasks)}
  function page(){seed();const tasks=AppStorage.load('schedule'),today=new Date().toISOString().slice(0,10);return `<div class="page-heading"><div><span class="eyebrow">Plano de 90 dias</span><h1>Planner de estudos</h1><p>Organize teoria, questões e revisões em uma carga que cabe na sua rotina.</p></div><div class="page-actions"><button class="button button-primary" data-action="open-task">+ Nova tarefa</button></div></div><div class="tabs"><button class="tab active">Hoje</button><button class="tab">Semana</button><button class="tab">Mês</button><button class="tab">90 dias</button></div><div class="grid grid-4">${metric('18h30','Meta semanal'),metric('100','Questões'),metric('4','Revisões'),metric('1','Simulado')}</div><div class="card" style="margin-top:18px"><div class="card-header"><div><h2>Agenda</h2><span class="card-subtitle">Todas as tarefas podem ser editadas</span></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Data</th><th>Atividade</th><th>Disciplina</th><th>Duração</th><th>Prioridade</th><th>Status</th><th>Ações</th></tr></thead><tbody>${tasks.slice(0,30).map(t=>taskRow(t,t.date===today)).join('')}</tbody></table></div></div><div class="modal" id="task-modal"><div class="modal-panel"><div class="modal-header"><h2>Planejar atividade</h2><button data-action="close-task">×</button></div><form id="task-form"><div class="field-row"><div class="form-group"><label for="task-date">Data</label><input class="input" type="date" id="task-date" required value="${today}"></div><div class="form-group"><label for="task-duration">Duração (minutos)</label><input class="input" type="number" id="task-duration" min="5" value="60"></div></div><div class="form-group"><label for="task-title">Título</label><input class="input" id="task-title" required></div><div class="field-row"><div class="form-group"><label for="task-discipline">Disciplina</label><input class="input" id="task-discipline" required></div><div class="form-group"><label for="task-type">Tipo</label><select class="select" id="task-type">${['teoria','questões','revisão','simulado','leitura','flashcards'].map(x=>`<option>${x}</option>`).join('')}</select></div></div><div class="form-group"><label for="task-notes">Observações</label><textarea class="textarea" id="task-notes"></textarea></div><button class="button button-primary" type="submit">Salvar tarefa</button></form></div></div>`}
  function taskRow(t,today){return `<tr data-task="${t.id}" ${today?'style="background:var(--blue-soft)"':''}><td>${new Date(t.date+'T12:00:00').toLocaleDateString('pt-BR')}</td><td>${Navigation.escapeText(t.title)}</td><td>${Navigation.escapeText(t.discipline)}</td><td>${Math.floor(t.duration/60)}h${String(t.duration%60).padStart(2,'0')}</td><td><span class="badge ${t.priority==='alta'?'danger':''}">${t.priority}</span></td><td><span class="badge ${t.status==='concluída'?'success':'warning'}">${t.status}</span></td><td><button data-action="task-complete" data-id="${t.id}">Concluir</button> <button data-action="task-delete" data-id="${t.id}">Excluir</button></td></tr>`}
  function metric(v,l){return `<div class="card stat-card"><span class="stat-icon">□</span><div><b>${v}</b><span>${l}</span></div></div>`}
  window.Planner={page};
})();
