(function(){
  let active=null;
  function page(){const notes=AppStorage.load('notes');return `<div class="page-heading"><div><span class="eyebrow">Seu espaço de síntese</span><h1>Anotações</h1><p>Registre ideias, resumos e conexões importantes.</p></div><div class="page-actions"><button class="button button-primary" data-action="new-note">+ Nova anotação</button></div></div><div class="filters"><input class="input" id="note-search" placeholder="Pesquisar anotações…"><select class="select" id="note-discipline"><option value="">Todas as disciplinas</option><option>Método e planejamento</option><option>Língua Portuguesa</option><option>Matemática</option><option>Informática</option></select></div><div class="grid grid-3" id="notes-grid">${notes.length?notes.map(card).join(''):`<div class="card empty-state" style="grid-column:1/-1"><b>Nenhuma anotação ainda.</b><p>Crie sua primeira síntese de estudo.</p></div>`}</div><div class="modal" id="note-modal"><div class="modal-panel"><div class="modal-header"><h2>Editar anotação</h2><button data-action="close-note">×</button></div><form id="note-form"><div class="form-group"><label for="note-title">Título</label><input class="input" id="note-title" required></div><div class="field-row"><div class="form-group"><label for="note-disc">Disciplina</label><input class="input" id="note-disc"></div><div class="form-group"><label for="note-tags">Etiquetas</label><input class="input" id="note-tags" placeholder="revisão, importante"></div></div><div class="form-group"><label for="note-content">Conteúdo (Markdown básico: **negrito**, *itálico*, # título)</label><textarea class="textarea" id="note-content" style="min-height:230px"></textarea></div><span class="save-status">Salvamento automático ao fechar</span><button class="button button-primary" type="submit">Salvar agora</button></form></div></div>`}
  function card(n){return `<article class="card note-card" data-note="${n.id}"><div class="card-header"><span class="badge">${Navigation.escapeText(n.discipline||'Geral')}</span><button class="icon-button" data-action="delete-note" data-id="${n.id}" aria-label="Excluir">×</button></div><h2 style="font-size:17px">${Navigation.escapeText(n.title)}</h2><p class="card-subtitle">${Navigation.escapeText(n.content).slice(0,160)}</p><div style="margin-top:16px"><button class="button button-secondary button-sm" data-action="edit-note" data-id="${n.id}">Editar</button></div></article>`}
  function open(id){active=id||null;const note=id?AppStorage.load('notes').find(n=>n.id===Number(id)):{};document.getElementById('note-title').value=note.title||'';document.getElementById('note-disc').value=note.discipline||'';document.getElementById('note-tags').value=(note.tags||[]).join(', ');document.getElementById('note-content').value=note.content||'';document.getElementById('note-modal').classList.add('open')}
  function save(){const note={id:active||Date.now(),title:document.getElementById('note-title').value.trim(),discipline:document.getElementById('note-disc').value.trim(),tags:document.getElementById('note-tags').value.split(',').map(x=>x.trim()).filter(Boolean),content:document.getElementById('note-content').value,date:new Date().toISOString()};if(!note.title)return;AppStorage.update('notes',n=>active?n.map(x=>x.id===active?note:x):[note,...n]);location.reload()}
  window.Notes={page,open,save};
})();
